import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ChangpassPage } from './changpass';

@NgModule({
  declarations: [
    ChangpassPage,
  ],
  imports: [
    IonicPageModule.forChild(ChangpassPage),
  ],
})
export class ChangpassPageModule {}
